#!/usr/bin/env bash
set -euo pipefail

project=/root/xiaozhi-autodl/dashboard
cd "$project"

if [[ ! -d node_modules ]]; then
  npm install
fi

npm run typecheck
npm run build
echo "Dashboard 已按当前源码构建完成"
